git init
git status
git add
git commit -m  //after running this commnads we can able to restore the file after deletion. with the help of the git restore fi                    name and we  can also commit the code at local repo.
git log
git log --online
git log --oneline --pretty
git restore commands.txt
git rm --cached
git config --global user.name
git config --global user.email
git commit -m after that we can maintain the versioning of the code. means we can main tain track the versioning code.

